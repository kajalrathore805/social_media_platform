


<%= form_with model: [ @post, @post.comments.build ] do |form| %>

  <%= form.hidden_field :commenter, value:@current_user.id  %>
  <p>
    <%= form.label :body %><br>
    <%= form.text_area :body %>
  </p>
  <p>
    <%= form.submit %>
  </p>
<% end %>
